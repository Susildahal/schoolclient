rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // DEVELOPMENT MODE - Allow all read/write operations
    // ⚠️ WARNING: This is NOT secure for production!
    // Replace with proper authentication before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }
    
    // PRODUCTION MODE (commented out) - Uncomment and modify when ready for production
    // match /settings/{document=**} {
    //   allow read: if true;  // Anyone can read settings
    //   allow write: if request.auth != null;  // Only authenticated users can write
    // }
    
    // match /teachers/{document=**} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    
    // match /testimonials/{document=**} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}
